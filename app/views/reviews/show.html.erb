<h1>Review for <%= @review.user.name %></h1>

<p><strong>Rating:</strong> <%= @review.rating %></p>
<p><strong>Comment:</strong> <%= @review.comment %></p>

<%= link_to 'Edit', edit_review_path(@review) %> |
<%= link_to 'Back to Reviews', reviews_path %> |
<%= form_with(model: @review, method: :delete, data: { confirm: 'Are you sure you want to delete this review?' }) do %>
    <%= submit_tag 'Delete', data: { confirm: 'Are you sure you want to delete this review?' } %>
  <% end %>