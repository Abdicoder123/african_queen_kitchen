<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>African Queen Kitchen</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Brand Colors */
    :root {
      --primary-color: #4b0082; /* Royal Purple */
      --secondary-color: #ffd700; /* Gold */
    }

    /* General Page Styling */
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #f8f9fa; /* Light gray for a clean look */
      color: #333;
    }

    /* Header Styling */
    h1 {
      text-align: center;
      color: var(--primary-color);
      margin-top: 20px;
      font-weight: bold;
    }

    p {
      text-align: center;
      margin: 10px 0;
    }

    /* Divider Styling */
    hr {
      border: 1px solid var(--primary-color);
      margin-bottom: 20px;
    }

    /* Card Container */
    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      padding: 20px;
    }

    /* Individual Card */
    .card {
      background-color: #fff;
      border: 1px solid var(--primary-color);
      border-radius: 10px;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      text-align: center;
    }

    /* Card Images */
    .card img {
      width: 100%;
      height: auto;
    }

    /* Card Information */
    .info-card {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 15px;
    }

    /* Buttons */
    .btn-primary {
      background-color: var(--secondary-color);
      color: #4b0082;
      border: none;
      font-weight: bold;
      margin-top: 10px;
    }

    .btn-primary:hover {
      background-color: var(--primary-color);
      color: #fff;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <h1>African Queen Kitchen</h1>
  <hr>
  <p>We are committed to helping you run your events with delicious food options and excellent service!</p>

  <!-- Card Section -->
  <div class="cards container">
    <!-- Card 1: Delicious Food -->
    <div class="card">
      <img src='https://developers.elementor.com/docs/assets/img/elementor-placeholder-image.png' alt="Delicious Food">
    </div>

    <!-- Card 2: Menu Info -->
    <div class="card info-card">
      <p>Check out our different menus suited for all kinds of events!</p>
      <a href="<%= menus_path %>" class="btn btn-primary">Menus</a>
    </div>

    <!-- Card 3: Event Catering -->
    <div class="card">
      <img src='https://developers.elementor.com/docs/assets/img/elementor-placeholder-image.png' alt="Event Catering">
    </div>

    <!-- Card 4: Reviews Info -->
    <div class="card info-card">
      <p>See what our clients are saying about us!</p>
      <a href="<%= reviews_path %>" class="btn btn-primary">Reviews</a>
    </div>

    <!-- Card 5: Tasty Dishes -->
    <div class="card">
      <img src='https://developers.elementor.com/docs/assets/img/elementor-placeholder-image.png' alt="Tasty Dishes">
    </div>

    <!-- Card 6: Inquiry Info -->
    <div class="card info-card">
      <p>Leave your events to us!</p>
      <a href="<%= new_order_path %>" class="btn btn-primary">Inquire</a>
    </div>

    <!-- Card 7: Event Setup -->
    <div class="card">
      <img src='https://developers.elementor.com/docs/assets/img/elementor-placeholder-image.png' alt="African Kitchen">
    </div>

    <!-- Card 8: About Us Info -->
    <div class="card info-card">
    <p> If you want to know more about us and why we do what we do click here.</p>
    <button class="btn btn-primary">About Us</button>
    </div>
  </body>
</html>
